<!DOCTYPE html>
<html
    lang="en-US">
    <head>
        <meta
            http-equiv="X-UA-Compatible"
            content="IE=edge"/>
        <meta
            charset="utf-8"/>
        <title>
            Landis Arboretum
        </title>
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"/>
        <meta
            name="referrer"
            content="origin-when-cross-origin"/>
        <script
            src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js"
            defer></script>

        <link
            rel="apple-touch-icon"
            sizes="180x180"
            href="/apple-touch-icon.png">
        <link
            rel="icon"
            type="image/png"
            sizes="32x32"
            href="/favicon-32x32.png">
        <link
            rel="icon"
            type="image/png"
            sizes="16x16"
            href="/favicon-16x16.png">
        <link
            rel="manifest"
            href="/site.webmanifest">
        <link
            rel="mask-icon"
            href="/safari-pinned-tab.svg"
            color="#5bbad5">
        <meta
            name="msapplication-TileColor"
            content="#da532c">
        <meta
            name="theme-color"
            content="#ffffff">

        <link
            rel="preconnect"
            href="https://fonts.googleapis.com">
        <link
            rel="preconnect"
            href="https://fonts.gstatic.com"
            crossorigin>
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Rosarivo&display=swap"
            rel="stylesheet">

        <link
            rel="stylesheet"
            href="/css/styles.css">
        <link
            type="text/css"
            rel="stylesheet"
            href="/js/lightgallery/css/lightgallery.css"/>

        <link
            type="text/css"
            rel="stylesheet"
            href="/js/lightgallery/css/lg-fullscreen.css"/>
        <link
            type="text/css"
            rel="stylesheet"
            href="/js/lightgallery/css/lg-thumbnail.css"/>

    </head>

    <body
        class="ltr">
        {% include '_partials/_header.twig' %}

        {% if category.featuredImage | length %}

            {% set optimizedImages = category.featuredImage.one().optimizedHero %}
            <div
                style="
                                    				        background-image:url('{{ optimizedImages.src() }}');
                                    				        background-position: center center;
                                    				        background-size: cover;
                                    				        background-repeat: no-repeat;
                                    				        width:100%;
                                    				        height:100%;
                                    				        ">

            {% endif %}
            <div
                class=" bg-opacity-50 bg-black px-4 py-24 md:py-60 "
                style="background:rgba(0,0,0,0.3);">
                <h1
                    class=" text-white text-4xl md:text-5xl md:max-w-5xl mx-auto text-center font-semibold">
                    {{ category.title }}
                </h1>

                <p
                    class="mb-5 uppercase text-white text-center text-2xl">
                    Volume
                    {{ category.volume }}
                    , Number
                    {{ category.number }}
                </p>

            </div>
        </div>

        <section
            class="max-w-7xl px-4 mx-auto mt-5 mb-10 min-h-screen">
            <div
                class="my-10">
                <p
                    class="uppercase text-gray-500">
                    In this Issue
                </p>
                {# Fetch all entries related to this category #}
                {% set entries = craft.entries()
  .relatedTo(category)
  .all() %}

                {# Output them #}
                {% for entry in entries %}
                    <a
                        href="{{ entry.url }}" class="text-blue-600 text-lg">
                        {{ entry.title }}
                    </a>
                    &nbsp;&middot;&nbsp;
                {% endfor %}
            </div>

            <div
                class="grid grid-cols-1 md:grid-cols-3 gap-x-4 gap-y-10 mt-20 ">
                {% for entry in craft.entries.section('news').relatedTo(category).all() %}

                    {# Fetch the entries #}

                    <div>
                        {% for myAsset in entry.featuredImage.all() %}
                            <a
                                href="{{ entry.url }}">
                                <img
                                    src="{{ myAsset.url('square500') }}" class=" mx-auto bg-gray-800 rounded-xl  hover:opacity-10">
                            </a>
                        {% endfor %}

                        <p
                            class="mb-0 text-base   text-gray-500 uppercase">
                            {{ entry.postDate | date("M d, Y") }}
                        </p>
                        <h4
                            class="mb-2 text-xl">
                            <a
                                href="{{ entry.url }}" class="text-blue-500 hover:text-blue-800 font-medium">
                                {{ entry.title }}
                            </a>
                        </h4>

                        {{ entry.textMarkdown | chop(limit=60, unit='w',append="...",allowedTags="<p>")  | retconAttr('p', { class: 'text-base mb-2 lg:text-lg' }) | widont }}

                        <a
                            href="{{ entry.url }}" class="inline-block border-b border-blue-700 hover:border-blue-900 border-dotted text-blue-500 hover:text-blue-800">
                            Read More
                        </a>

                        {# PurgeCSS: bg-green-600 text-base md:text-lg mx-auto max-w-3xl py-4 px-6 rounded-2xl my-10 text-white px-8 #}

                    </div>
                     {% endfor %}
                </div>

           
        </div>
        <div
            class="my-10 mx-auto max-w-5xl">
            <p
                class="italic leading-tight">
                Your membership dollars are critical to our economic viability and the sustainability of our collections, grounds, and educational programs. You can help by
                <a
                    href="/membership"
                    class="text-blue-600">
                    joining or renewing your membership
                </a>
                .Consider renewing at a higher level. Encourage your family, friends, neighbors, and co-workers to do so as well.  Give the gift of membership to the beautiful Landis Arboretum!
            </p>
        </div>

        <div
            class="bg-green-200 py-48 grid grid-cols-4">
            <div>
                <h3>
                    Sponsor partial here.
                </h3>
            </div>
        </div>
    </body>
</html></section>{# PurgeCSS: text-base lg:text-lg #}{% include '_partials/_footer.twig' %}</body></html>
