{% include '_partials/_head.twig' %}

<body class="ltr">
	{% include '_partials/_header.twig' %}

	{% if entry.featuredImage | length %}

		{% set optimizedImages = entry.featuredImage.one().optimizedHero %}
		<div
			style="
				        background-image:url('{{ optimizedImages.src() }}');
				        background-position: center center;
				        background-size: cover;
				        background-repeat: no-repeat;
				        width:100%;
				        height:100%;
				        ">

		{% endif %}
		<div class="px-4 pt-24 bg-black bg-opacity-60  md:py-48" style="background:rgba(0,0,0,0.3);">
            <h1 class="mx-auto text-3xl font-semibold text-center text-white  md:text-4xl lg:text-5xl md:max-w-5xl mb-2">
                {{ entry.title | widont }}
            </h1>
            <p class="mt-0 text-xl font-semibold text-center text-white md:text-2xl">
                {{ entry.subtitle }}
            </p>
        </div>
    </div>




	<section class="max-w-4xl min-h-screen px-4 py-10 mx-auto lg:max-w-5xl">

		<article class="mx-auto">

	<h1 class="mb-12">Maps and Guides</h1>

			        <h2 class="mb-10 text-gray-700 text-xl md:text-xl mx-auto">{{ entry.textMarkdown }}</h2>

		</article>


		<section class="px-4 mx-auto mt-10 max-w-7xl">

			<div class="grid grid-cols-1 md:grid-cols-2 md:gap-y-20 md:gap-x-6">

				{% set myEntryQuery = craft.entries()
					.section('mapItem')
					.all() %}

				{% for entry in myEntryQuery %}

					<div class="flex bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300 h-full ">
						<div class="w-1/3 flex flex-col ">
							{% if entry.featuredImage | length %}
							{% for myAsset in entry.featuredImage.all() %}
								<a href="{{ entry.url }}" class="flex">
									<img src="{{ myAsset.url('square500') }}" class="w-32 md:w-40 aspect-square object-cover rounded-l-lg shadow-sm hover:opacity-90 transition-opacity duration-300">
								</a>
							{% endfor %}

							{% endif %}
						</div>

						<div class="w-2/3 pl-4 md:pl-6 pt-4  flex flex-col justify-between">
							<div>
								<h4 class="text-lg font-medium leading-tight md:text-xl mb-2">

									<a href="{{ entry.url}}" class="text-blue-500 hover:text-blue-800 transition-colors duration-300">{{ entry.title }}</a>
								</h4>
								<p class="text-sm md:text-base leading-relaxed mb-2">
									{{ entry.textMarkdown  }}
								</p>
							</div>
							<p class="text-sm">
								<a href="{{ entry.resourceLink }}" class="text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 py-1 px-2 text-sm rounded-md">
									View/Download
								</a>
							</p>
						</div>

					</div>

			{% endfor %}

			</div>

		</section>

	</section>



	{% include '_partials/_footer.twig' %}
    				<script src="/js/lightgallery/lightgallery.min.js"></script>
                				<script src="/js/lightgallery/plugins/zoom/lg-zoom.umd.js"></script>

									<script src="/js/lightgallery/plugins/fullscreen/lg-fullscreen.umd.js"></script>
									<script src="/js/lightgallery/plugins/thumbnail/lg-thumbnail.umd.js"></script>

        			<script type="text/javascript">
			lightGallery(document.getElementById("lightgallery"), {
  speed: 500,
  plugins: [lgZoom,lgFullscreen],
  showZoomInOutIcons: true,
  actualSize: true,
  zoom: true,
  thumbMargin: 20,
  thumbHeight: 120,
  thumbWidth: 120,
  download: true,
  actualSizeIcons: { zoomIn: 'lg-actual-size', zoomOut: 'lg-zoom-out' }
});



			</script>
</body>
</html>
